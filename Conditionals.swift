//Comparisons
var higherValue = 200
var lowerValue = 100

higherValue == lowerValue // equal to
higherValue != lowerValue // not equal
higherValue <= lowerValue // less than or equal to lower value
higherValue >= lowerValue // greater than or equal to lower value
higherValue > lowerValue // greater than lower value
higherValue < lowerValue // less than lower value

//Ternary
var points = higherValue < 50 ? 40 : 20

//Logical
var isJogging = true
var isJumping = !isJogging
isJogging && isJumping // AND
isJogging || isJumping //OR



